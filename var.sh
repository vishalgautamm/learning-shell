#!/bin/sh
# variable GREET stores the message
## a variable can only hold one value, so a string with spaces must be quoted to that the shell knows to treat it all as one

GREET="Hello world"
echo $GREET

